spring:
  # ?????
  datasource:
    url: jdbc:mysql://192.168.126.128/mydb?allowPublicKeyRetrieval=true&characterEncoding=utf8&useSSL=false&serverTimezone=UTC&characterEncoding=utf-8
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 123456

  # JPA ??
  jpa:
    hibernate:
      # create???????create-drop??????????update ??????????validate??????????
      ddl-auto: update
    # ??sql
    show-sql: true
    # ???????
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect

# ?? Knife4j ???????????
knife4j:
  enable: true

logging:
  level:
    org.hibernate.type.descriptor.sql.BasicBinder: trace
    root: info

# WEB ??
server:
  port: 8080
  servlet:
    context-path: /springboot
    session:
      timeout: 60
